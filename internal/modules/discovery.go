// Package modules handles discovery and loading of dashboard UI modules.
// It scans the modules directory for module.json manifests, parses them,
// builds the navigation menu, and resolves MQTT topic subscriptions.
package modules
